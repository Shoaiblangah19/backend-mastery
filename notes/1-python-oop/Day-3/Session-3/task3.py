'''
## ⚡ Micro Task 3 (5 min)

Look at these methods and decide: instance, classmethod, or staticmethod? Don't write code — just classify each and explain why in one line:

1. `calculate_age(birthdate)` → returns age from birthdate -> static method
2. `get_full_name()` → returns first + last name from object -> instance method
3. `from_json(json_string)` → creates object from JSON -> class method
4. `is_valid_url(url)` → checks if string is valid URL -> static method
5. `deactivate()` → sets object's is_active to False -> instance method
6. `get_total_count()` → returns count from class variable  -> class method
7. `format_currency(amount, currency)` → returns "$99.99" format -> static method
'''
